{
  "name": "warble-dictate",
  "version": "0.1.0",
  "private": true,
  "description": "System-wide dictation for Warble: menu bar app, global hotkey, paste-into-focused-app.",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.main.json && tsc -p tsconfig.renderer.json && mkdir -p dist/renderer && cp src/renderer/index.html src/renderer/style.css dist/renderer/",
    "start": "npm run build && electron ."
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "electron": "^33.2.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "@nut-tree-fork/nut-js": "^4.2.0"
  }
}
